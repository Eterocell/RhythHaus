## 1. Import contract and pure policy

- [x] 1.1 Add the narrow iOS import provider/completion ABI and result-status constants; verify the shared JVM tests compile and map success, cancellation, unavailable, overlap, and failure statuses without exposing external URLs.
- [x] 1.2 Add pure managed-destination filename sanitization and deterministic collision policy; verify JVM tests preserve supported extensions, reject path traversal/separators, distinguish byte-identical duplicates, and choose stable suffixes for different content.

## 2. Kotlin iOS launcher integration

- [x] 2.1 Replace the iOS app-local-only folder launcher with an iOS import launcher that invokes the retained provider, returns the existing `IosAppLocal` source after successful or duplicate-only import, and leaves Android/JVM launchers unchanged; verify focused common/JVM launcher tests.
- [x] 2.2 Wire import status and count summaries into existing Shared picker/orchestration state without starting a scan after cancellation or failure; verify existing Library/App state tests cover success, cancellation, failure, and mutation gating.

## 3. Swift Files.app provider

- [x] 3.1 Add a Swift document-picker provider supporting multiple audio files and directories, balanced security-scoped access, recursive enumeration, supported-extension filtering, and idempotent operation state; verify Swift host tests cover picker cancellation, unavailable presenter, stale delegate callback, and overlapping launch.
- [x] 3.2 Copy selected files atomically into the managed app-local music directory with deterministic collision handling and duplicate counts; verify Swift host tests cover nested directories, unsupported files, same-content reimport, different-content filename collision, and copy failure aggregation.
- [x] 3.3 Register the provider in the iOS bootstrap and update the visible import copy to explain Files.app selection and sandbox copying; verify the iOS host target compiles and the marker/import text is localized or consistently user-facing.

## 4. Verification and acceptance

- [x] 4.1 Run focused common/JVM and iOS host tests, then `./gradlew :shared:jvmTest --configuration-cache`, `./gradlew :feature:library:impl:jvmTest --configuration-cache`, `./gradlew :desktopApp:compileKotlin --configuration-cache`, `./gradlew :androidApp:assembleDebug --configuration-cache`, `/usr/bin/xcrun xcodebuild -version`, and `./gradlew :shared:iosSimulatorArm64Test --configuration-cache`.
      Completed automated evidence: focused Kotlin regressions `./gradlew :shared:jvmTest --tests 'com.eterocell.rhythhaus.library.PlatformSourceAccessTest.identicalBytesInOccupiedSuffixAreReturnedAsDuplicate' --tests 'com.eterocell.rhythhaus.AppLibraryImportTest.successfulImportKeepsMutationsExcludedUntilFollowUpScanIsAdmitted' --tests 'com.eterocell.rhythhaus.AppLibraryImportTest.cancelledFollowUpScanReleasesPendingGateAndStaysSilent' --configuration-cache` -> `BUILD SUCCESSFUL`; focused Swift Xcode run on the available `iPhone 17, OS=26.5` simulator, 5 selected tests, 0 failures (suffix duplicate, batch fold, locale marker, child/root directory-symlink protections); full `:shared:jvmTest`, `:feature:library:impl:jvmTest`, `:desktopApp:compileKotlin`, `:androidApp:assembleDebug`, and `:shared:iosSimulatorArm64Test` all with `--configuration-cache` -> `BUILD SUCCESSFUL`; `xcodebuild -version` -> Xcode 26.6, build 17F113; `spotlessApply` repaired formatting only in `shared/src/commonMain/kotlin/com/eterocell/rhythhaus/App.kt`, `shared/src/commonTest/kotlin/com/eterocell/rhythhaus/AppLibraryImportTest.kt`, and `shared/src/commonTest/kotlin/com/eterocell/rhythhaus/library/PlatformSourceAccessTest.kt`, followed by `spotlessCheck`, `detekt`, `architectureCheck`, and `openspec validate ios-files-import --strict` -> `BUILD SUCCESSFUL`/valid; Android D8 root cause repair renamed only the Shared Android/JVM facade Kotlin files to `PlatformFolderPickerFacade.android.kt` and `PlatformFolderPickerFacade.jvm.kt`, eliminating the duplicated generated file class while preserving actual API/delegation, and `:androidApp:assembleDebug` passed after that repair with the scoped independent review reporting no Critical/Important findings.
- [x] 4.2 Manual Files.app release acceptance: on an iOS simulator or device, exercise the real document picker and verify file/directory selection, folder recursion, cancellation, and repeated import, then scan, app restart, and playback of the copied audio; record the evidence. The automated harness exercised the provider and policy but did not interact with a real Files.app document picker.
  - 2026-09-09 user acceptance: verified the real Files.app flow with playable audio — file and directory selection, recursive import, cancellation, repeated import, scan, app restart, and playback of the copied file all behaved correctly.
- [x] 4.3 Add the iOS default `Documents/RhythHaus` source lifecycle: first launch registers and scans only the missing stable `ios-app-local` source; existing source records remain untouched. Add an already-managed import status so selecting the managed directory, a descendant, or an existing managed audio file starts a scan without copying. Verify focused common/JVM orchestration/mapping tests and Swift path-containment/copy tests.
  - 2026-09-09 automated evidence: focused Shared JVM source-registration/status-mapping regressions passed; focused `iosAppTests` path-containment and managed-directory no-copy XCTest cases passed on `iPhone 17, OS=26.5`; and `IosLibraryImportLauncherIosTest.defaultSourceExposesTheExistingIosManagedDocumentsDirectory` passed through `:shared:iosSimulatorArm64Test`.
- [x] 4.4 Close out: validate `ios-files-import --strict` against the final diff, review the diff against both delta specs, and commit/archive the completed change with a conventional message only after the 4.3 automated evidence is recorded.
  - 2026-09-09 evidence: strict change validation, main-spec validation after delta sync, all-platform `./init.sh`, separate Spotless/Detekt gates, and `git diff --check` passed. An independent targeted review found no Critical or Important issue in the default-source lifecycle or managed-directory no-copy boundary.
