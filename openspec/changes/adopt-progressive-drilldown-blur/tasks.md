## 1. Progressive Effect Policy

- [x] 1.1 Add failing core-ui common tests for uniform and top-edge blur-style selection, including the exact `ProgressiveBlur.Top` composite contract; verify the focused test fails before production implementation and succeeds after it.
- [x] 1.2 Add the local blur-style policy and implement the matching Miuix progressive effect/composite pair in `rhythHausLiquidGlass`, retaining the existing uniform default and no-effect fallback branch; verify `./gradlew :core:ui:jvmTest --tests '*LiquidGlassChromeTest*' --configuration-cache`.

## 2. Drill-Down Integration

- [x] 2.1 Opt only `DrillDownMiuixScrollChrome` into the top-edge style; preserve Now Playing and every other existing caller on the default uniform style; verify the Library JVM production-composable regression proves existing detail controls/scroll behavior still work.
- [x] 2.2 Add or extend the JVM UI regression that verifies the production drill-down style policy is top-edge progressive while preserving its existing Back-button and scroll assertions; the core-ui policy suite guards the floating-bar uniform default; verify `./gradlew :feature:library:impl:jvmTest --tests '*DrillDownViewJvmTest*' --configuration-cache`.

## 3. Generated Metadata and Acceptance

- [x] 3.1 Run `./gradlew :shared:exportLibraryDefinitions --configuration-cache` and accept only generator-produced AboutLibraries updates that describe the resolved `0.9.4-rc01` Miuix artifacts. Controller evidence: `BUILD SUCCESSFUL` (10s); generated Miuix entries `miuix-blur`, `miuix-core`, `miuix-preference`, `miuix-shader`, `miuix-squircle`, and `miuix-ui` report `0.9.4-rc01`.
- [x] 3.2 Run focused regression suites, inspect the desktop application’s detail scroll surface on an actual supported renderer, then run `./gradlew spotlessApply --configuration-cache`, `./gradlew spotlessCheck --configuration-cache`, `./gradlew detekt --configuration-cache`, `./gradlew :shared:jvmTest :desktopApp:compileKotlin :androidApp:assembleDebug --configuration-cache`, `/usr/bin/xcrun xcodebuild -version`, and `./gradlew :shared:iosSimulatorArm64Test --configuration-cache`. Controller evidence: focused `:core:ui:jvmTest --tests '*LiquidGlassChromeTest*'` and `:feature:library:impl:jvmTest --tests '*DrillDownViewJvmTest*'` both passed; `spotlessApply` `BUILD SUCCESSFUL` (20s); `spotlessCheck` `BUILD SUCCESSFUL` (19s); `detekt` `BUILD SUCCESSFUL` (2s); shared JVM/desktop/Android command `BUILD SUCCESSFUL` (48s); Xcode 26.6, build 17F113; shared iOS simulator command `BUILD SUCCESSFUL` (1m 7s), with pre-existing no-cast-needed compiler warnings in `AppScanCancellationTest.kt`. Desktop smoke was partial: real `:desktopApp:run` launched, Orca opened an album detail in wide master/detail, and a screenshot confirmed the master pane remained visible and the floating Now Playing bar had uniform blur. Orca’s synthetic scroll/drag did not move the Compose detail and keyboard input could not focus its window, so the collapsed progressive top edge was not visually confirmed; this is an exact limitation, not a failure claim.
- [x] 3.3 Review the final diff against this change’s requirements, update `progress.md` and `roadmap.md` with exact verification evidence, validate with `openspec validate adopt-progressive-drilldown-blur --strict`, and commit the completed OpenSpec implementation with a conventional message.
