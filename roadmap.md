- [x] 架构重构 Slice 5 Task 5.4 accepted：implementation `862a6c4` -> `575e41c` -> `f542a1d` extracted unexported `:feature:settings`; Shared retains routes/Back/mutation/dialog/catalog authority via `commonMainImplementation`. Final review `PASS / APPROVED`; exact extraction manifest 23 endpoints. Retained XML: focused Settings JVM `20/0`, Android/iOS policy `3/0`/`3/0`, Shared JVM `311/0/0/0`, iPhone 17 XCTest `8/0`; full feature-JVM matrix log passed but its XML was overwritten, so no full retained count is claimed. Architecture/quality/strict OpenSpec/consumer/Xcode/`./init.sh` logs passed; evidence closeout committed as `850017b56035413ac2aa9cce727bb0b106af37cd`. Physical-device/runtime, desktop UI, visual/accessibility, picker/scanner, and playback interaction remain unverified.
- [x] 架构重构 Slice 5 Task 5.3 accepted：implementation commit `90e330d24b10b9668263002b9cc37945d24e9643`（`refactor: extract search feature`，direct planning parent `f947724a9a2a29e5863976cb2c17fc16225bd336`）将 Search 提取为 unexported `:feature:search` callback-first leaf；shared 保留 route/Back、selection、scroll、playback、bottom-bar、Now Playing 和 Equalizer policy。final behavior/spec/quality 与 exact 20-path scope reviews 均为 `PASS / APPROVED`。Post-review acceptance ran on the final uncommitted 20-endpoint snapshot; before/after hashes proved byte continuity and those exact bytes were committed as `90e330d`。Search filter JVM/Android/iOS `3/0/0/0` each、Search JVM UI `14/0/0/0`、Shared Home/SearchRoute/Settings `1/7/6` all zero failures、Shared JVM/iOS `311/0/0/0`/`236/0/0/0`、architecture functional `82/0/0/0`；twice-reused architectureCheck、Spotless、Detekt、strict named OpenSpec、Android assemble/desktop compile、generic Xcode Simulator build、iPhone 17 tests `8/0`、`./init.sh` within 20 minutes、diff/exact-20-path gates passed。physical-device runtime、desktop UI launch、visual/accessibility QA、live scanning 与 playback-engine runtime interaction 未验证；evidence closeout commit pending。下一项：open Task 6.4 Settings extraction。
- [x] 架构重构 Slice 5 Task 5.2 accepted：implementation commit `fc1b96f858408c8dfd07221d5fe85ae3e20ced63`（`refactor: extract playlists feature`，base `c0e1e7b9d07679d7beecd530d1958e50b58b1e3`）将 playlists/backup UI、state/reducer/owner、repository implementations/binding、codec/service、neutral document seam、Android/JVM launchers、resources 与 tests 提取至 `:feature:playlists:impl`；shared 保留 composition、shell/routes/Back、lifecycle、Koin assembly 与 retained iOS ABI facade/adapter。独立 combined review `PASS / APPROVED`，平台 facade correction 已解决历史 D8 duplicate 与 Swift owner 问题。最终 XML：feature JVM/Android-host/iOS `115/93/83`、Shared JVM/iOS `307/236`、core DB JVM `3`、Settings `5`、Xcode `8/0`；architecture functional `71` tests with `6` expected skips，所有 stated failure/error 为 0，relevant skip 为 0。Convention/architecture/quality/OpenSpec/95-path/diff gates、generic unsigned Xcode Simulator build、exact iPhone 17 test 与 `./init.sh` 均通过。Task 5.2 evidence closeout 已作为 `6e885ef75ada0d6e48b2832cb3852b460a6c62ed`（`docs: close playlists feature extraction`）提交，不再 pending。明确不声称 Android/iOS physical-device runtime、desktop UI launch、rendered visual QA、live picker/document interaction 或 playback runtime；下一项：Task 5.3 Search discovery/planning。
- [x] 添加循环播放选择, 随机播放选择
- [x] 架构重构 Slice 5 Task 5.1 accepted：implementation commit `28dd2e1`（`refactor: extract now playing feature`）将 Now Playing 提取为 UI-only `:feature:nowplaying`；shared 保留 route/Back/shell/composition，`:core:ui` 接管批准的 generic gesture/glass moves。Gradle 9.6.1 absent-module RED 的实际 task-selection 文本与计划 exact string 不同，但 controller 因 module 缺失导致 task selection failed、且未执行 requested feature task 或 compilation 而接受语义等价，不声称 exact string 出现。Independent scope/behavior approval 已完成：scope `ses_032e868eeffei1GpxKbH327EB1` PASS against `96cb487`、behavioral `ses_0328e9e86ffeRFAqPeTnpxV5pX` `Findings: None. PASS / APPROVED` 审阅的是 pre-commit 38-path worktree，随后成为 `28dd2e1`，不是 post-commit SHA。focused Now Playing JVM XML 为 18/18（Content 3、Artwork Rendering 12、Bar Semantics 3），gesture 11/11、architecture fixture 65/65，均零 skipped/failures/errors；gesture cancellation 是 direction-specific：terminal-side 可 callback 并 settle terminal，nonterminal-side settle opposite 且无 callback，drag/rubber-band/threshold/spring math 未变。`LibraryDetailContent.kt` 与 `ArtworkImage.kt` 均为 inspected-but-unchanged approved paths，不在 38-path staging set。Android-host/iOS-simulator、feature/core consumer/platform compilation（141 actionable：13 executed、128 up-to-date）、Xcode 26.6、twice-reused architectureCheck、Spotless/Detekt/named strict OpenSpec/diff 均通过。shuffle/Previous nondeterministic control-test finding 已用 shuffle Off 的 deterministic transport assertions 关闭，未改 production behavior。`openspec validate --all --strict` 不作通过声明：retained 44 passed/1 unrelated pre-existing `spec/ios-now-playing-info` failed。`./init.sh`、runtime UI/playback、desktop launch、Android/iOS device/runtime validation 未运行；OpenSpec 4.4 保持 open。
- [x] 架构重构 Slice 4 Task 4.2 accepted：implementation commit `ab1768c`（`refactor: extract playback contracts`）提取 `:core:playback`，accompanying separate evidence-closeout package 携带 tracked ledgers 与 force-added ignored evidence files，不声明 docs SHA。`:core:playback` 原子拥有 package-stable playback contracts/controller/session value+codec、platform engines/service/bridges/native helper、factories 与 moved tests，仅依赖 approved core model/platform；shared 保留 session coordinator/store/DataStore/process lifecycle/Koin/App orchestration 与 package-stable platform-factory facade，仍是唯一 Xcode-facing framework 并 narrowly export core playback。独立 re-review 为 no findings、SPEC PASS、QUALITY APPROVED，先前 behavioral-KDoc finding 已解决。Controller consumer matrix（289 actionable，cache reused）、fresh core/shared tests（122 executed）、external-JAR architecture functional XML 60/0/0/0、twice-reused architectureCheck、Xcode 26.6 iOS Simulator consumer build、iOS simulator、Spotless/Detekt/OpenSpec/diff 全部通过；PlaybackController JVM/Android-host/iOS XML 为 53/53/53、均零 skipped/failures/errors。`./init.sh`、desktop runtime、Android/iOS runtime/device media validation 未运行，不作 runtime claim；OpenSpec 4.4 保持 open。
- [x] 架构重构 Slice 4 Task 4.1 accepted：commit `9bd972f`（`refactor: publish library and playlist APIs`，base `066e592`）新增 package-stable `:feature:library:api`（完整 13-method Library contract，仅依赖 `:core:model`）与 `:feature:playlists:api`（完整 11-method contract，返回 `PlaylistSummary`，无 production project dependency）；shared 继续拥有 repositories、mappers、Koin factories，未创建 physical implementation modules 或 iOS framework export。Architecture negative/positive controls complete；corrected-snapshot review `review-066e592..58937c7.diff` 为 SPEC PASS/QUALITY APPROVED，无 Critical/Important/Minor 且 prior findings 全部解决。Controller Spotless、276-task API/shared/Android/desktop/iOS compile matrix、Xcode/shared iOS cache reuse、twice-reused architectureCheck、standalone Spotless/Detekt、strict OpenSpec/diff 均通过；XML 为 Library API 5/5/5、Playlist API 1/1/1、shared JVM 562、architecture functional 57，均零 failure/error。`ktlintFormat` unavailable 的诚实结果保留。`./init.sh` script、Android/desktop/iOS runtime UI launches、OpenSpec 4.4 与后续 implementation-module/facade slices 仍 deferred。
- [x] 修复即使上一页 NowPlayingBar 仍然 visible 的情况下, 从 NowPlayingScreen 返回也会显示 NowPlayingBar hidden->visible 的动画的问题
- [x] 修复按钮中字母 'g' 的下方可能被截断的问题
- [x] 添加 Nested Scroll 效果给 Track list 和 Library 等页面, 类似于 Material 3 Expressive 的效果, 使用 miuix-blur 添加模糊效果
- [x] Miuix 组件迁移：添加 miuix-preference，将 Settings 外观选择迁移到 OverlayDropdownPreference，将 Search 输入迁移到 Miuix TextField/IconButton；保留音乐专属的行、封面、均衡器和路由弹窗壳体自定义
- [x] Miuix TopAppBar 迁移：新增 `RhythHausTopAppBar` wrapper，将 Search、Settings、Library drill-down 的普通返回/标题栏替换为 Miuix `SmallTopAppBar`，保留 nested-scroll glass chrome 与 Now Playing 自定义
- [x] Miuix nested-scroll TopAppBar 迁移：Library home/drill-down collapsed glass chrome 的标题内容改用 Miuix `SmallTopAppBar` 路径，保留 RhythHaus glass/backdrop、状态栏覆盖与滚动触发逻辑
- [x] MiuixScrollBehavior drill-down title collapse：drill-down track list 改用 Miuix `MiuixScrollBehavior().nestedScrollConnection` + direct Miuix `TopAppBar(title, largeTitle, scrollBehavior)`，由 Miuix 负责大标题到 collapsed topbar 标题/返回按钮的过渡；Library home 暂保留既有 `NestedScrollBlurChrome`。验证：shared compile、LibraryNavigationTest；仍需设备/模拟器手动调 top padding/标题位置。
- [x] i18n
- [x] 架构重构：拆分 App.kt，提取 Library 导航/状态/路由/页面内容到独立文件 (commits 7c7e895..0cbe270)
- [x] 包结构整理：将 Library UI、NowPlaying/Search/Settings、通用 UI 与 theme  helpers 迁入 feature-first 包 (commits f0310e5..adb1e3d); playback/model 包迁移因 Playback.kt 混合模型与引擎逻辑且涉及 AndroidManifest/iOS Swift 桥接而 deferred
- [x] 修复 iOS 锁屏时曲末可能不自动播放下一首：iOS 播放改由 Swift `AVAudioPlayerDelegate` 事件回调驱动完成通知，Kotlin 轮询只保留进度更新；仍需真机锁屏多曲目手动验证
- [x] Album/artist drill-down top-bar artwork：album/artist track-list Miuix TopAppBar 现在会在有 embedded artwork 时未滚动时用方形代表性封面，滚动后过渡为矩形顶部栏封面；有封面时移除顶部栏 blur/glass，返回按钮使用圆形背景，album/artist 名称使用 chip 风格背景；无封面或解码失败时保持原有 glass 标题/返回行为，不显示字母占位。
- [x] Coil artwork loading + Koin DI：添加 Coil 3 KMP 作为 shared Compose artwork 加载/缓存路径，track row、album/artist、drill-down top bar、NowPlayingBar/Screen artwork 改走稳定 cache key 的 `ArtworkImage` helper；添加 Koin 4 shared module 并从 Android/desktop/iOS 启动入口初始化，`App()` 通过 Koin 获取现有 service graph。仍需目标设备/模拟器手动确认 artwork 加载视觉与缓存体感。
- [x] 除 iOS 因平台限制以外，Android 与 desktop JVM/macOS 可重复选择、重扫及移除多个独立音乐库文件夹，含空库卡片 progress+job gate、Android SAF 只读权限/移除或清空后释放、完整 URI/路径无碰撞 identity 与 legacy exact-handle 复用、generic picker error、child-first transactional clear 和取消资源清理；iOS 仍限单一 app-local source。JVM/desktop/Android 验证通过；JVM 兼容修复已将历史 `user_version=0` 数据库在精确表结构识别后升级为可由 SQLDelight 正常打开的版本，避免 schema create 与既有 playlist 表冲突。完成扫描的 source 元数据更新改为 API-29-compatible 的 atomic insert-ignore/update，避免 SQLite REPLACE 删除父 source 并级联清空新扫描的 tracks/sessions/errors；真实 desktop Zemeth 扫描后已验证保留 92 tracks、completed session 与 scan errors。iOS simulator tests 已通过。后续非阻塞风险：JVM symlink containment/cycle detection、lossless `sourceLocalKey`、persisted terminal message sanitization；仍需设备手动验证。
- [x] UI/UX: 去除首页的 Nested Top Bar；首页保留 system-bar safe top inset 与原有内容 Header，不添加替代 top bar/blur/collapse；设置页保留 `safeContentPadding()`，将 app-owned spacing 收紧为 horizontal 16dp、vertical 8dp、item 12dp、bottom 8dp，并清除 Settings TopBar title/navigation 与外观选项重复叠加的 horizontal component padding，使其与“管理音乐”对齐，同时保留外观选项 16dp vertical inside margin 与返回按钮 44dp target。JVM、desktop、Android 验证通过；iOS tests 仍受既有 common-test `Thread` 不兼容阻塞，目标设备/窗口视觉 QA 仍需手动确认。
- [x] UX: 当前曲目行选择从零开始并播放；非当前选择以首页、专辑、艺人和 Search 的精确可见队列播放；专用播放/暂停仍切换，循环/随机保持。JVM、desktop、Android 和 OpenSpec 通过；iOS simulator 仍受既有 `AppScanCancellationTest.kt:56:28`/`:99:27` 的 `Thread` 引用阻塞；仍需手动可听播放/队列 QA。
- [x] feat: 记住上次的播放列表、当前曲目、播放进度、循环播放与随机播放设置；恢复后始终保持暂停且不自动播放。JVM、Android host、desktop 与 Android 验证通过，最终 Oracle release gate PASS；iOS simulator tests 仍受既有 `AppScanCancellationTest.kt:64`/`:340` JVM-only `Thread` 引用阻塞，iOS main compilation 已通过。
- [x] feat: 设置新增"关于"页面（Logo、应用名、生成版本号、指向 https://github.com/Eterocell/RhythHaus 的源码入口）与"开源库"页面（AboutLibraries 15.0.3 Material 3 目录，含加载失败重试、宽屏 720dp 可读宽度及手动 override 补全原生 TagLib 归属）。JVM、Android host、desktop、Android、iOS main 与 OpenSpec 验证通过；iOS simulator tests 仍受既有 `AppScanCancellationTest.kt` JVM-only `Thread` 引用阻塞；仍需目标设备/模拟器手动视觉 QA。
- [x] UX: Now Playing bottom bar 在 `LibraryRoute.Settings`、`LibraryRoute.SettingsAbout` 与 `LibraryRoute.OpenSourceLibraries` 隐藏，其他既有路由仍遵循原滚动可见状态；隐藏动画使用实测 bar 高度移动完整交互区域，避免透明残留区域拦截点击。OpenSpec、focused JVM、完整 JVM/desktop/Android 验证通过，iOS simulator tests 受 `AppScanCancellationTest.kt:64:28`/`:340:27` JVM-only `Thread` 引用阻塞，路由切换动画仍需目标设备/窗口手动视觉 QA。
- [x] fix(UI/UX): Track List 专辑/艺术家页在 lazy artwork 解析为可用 bytes 后，使用同一像素几何协调封面、列表与 TopBar，先逐像素折叠封面再滚动列表，并在列表回到顶部后反向展开；loading、缺失或加载失败则保留 Miuix 路径。OpenSpec、focused tests、完整 JVM/desktop/Android 与最终 Oracle gate 通过；iOS simulator tests 受既有 `AppScanCancellationTest.kt:64:28`/`:340:27` JVM-only `Thread` 引用阻塞。已取得 compact/wide、专辑/艺术家及无封面运行时截图，但自动手势与像素/CJK 审阅未能可靠验证，仍需人工完成运行时视觉 QA。
- [x] build(android): 支持 opt-in SplitAPK：仅 `rhythhaus.android.splitApk=true` 生成 `arm64-v8a`、`armeabi-v7a`、`x86_64` 与 universal 四个直下载 APK，默认/非精确值及 debug 保持单一 ordinary APK；Google Play AAB 独立生成并由 SDK `aapt2`/`apkanalyzer` 验证 canonical metadata。APK 签名验证为 `signed: verified`，APK/AAB configuration cache 均完成 stored→reused 验证，JVM/desktop/Android 回归通过；`./init.sh` 仍受既有 iOS common-test `Thread` 引用阻塞，设备安装/升级仍需手动验证。
- [x] feat: 播放列表页面提供 Saved/Queue 双标签、持久化创建/重命名/删除、重复且有序的条目、两种添加流程、可访问重排，以及当前项固定的可编辑后续队列；播放队列与会话使用 occurrence ID，来源删除/清库先完成 SQLDelight 级联，再协调播放并发布列表。OpenSpec 29/29、迁移、JVM、Android host、desktop、Android 与完整 iOS simulator 验证通过，`LibraryDatabaseIosTest` 实际执行 1/1 并证明生产 iOS 外键。Controller 最终全分支审查五条 lane 均 PASS（Goal/QA/Code Quality/Context 为高置信度，Security 最大 LOW），无 Critical/Important/Medium；LOW 后续为 repository 名称/列表/数量限制与 codec 对齐，以及 checkpoint queue 有界化/合并，不在本次证据收尾中实现。已实际验证 800x600 中文 Saved 空状态及 Queue 固定当前项、可访问移动、移除和清空确认；wide、light/dark、英文、完整 Saved CRUD/重复/两种添加/拖拽、键盘提交、clear 执行/陈旧命令拒绝、像素级 Now Playing/CJK 与目标设备可听播放仍为 INCONCLUSIVE 的未验证后续证据，不作为通过项。Generic Task 2 已恢复为无关的 pre-playlist 证据，queue 无障碍措辞已修正为 role-free content/state semantics 与独立 artwork image semantics。
- [x] UI/UX: 统一 Clear Library、Remove Folder、播放列表/picker 与 Clear Upcoming 到共享实色 `HausDialog`，并对齐播放列表 20dp inset、Saved/Queue 对比度及紧凑文本指标。严格 OpenSpec、JVM/desktop/Android、Xcode 与完整 iOS simulator tests 已通过；JVM Compose UI test 已实际调用 `SemanticsActions.Dismiss` 并验证回调仅一次且 dialog 移除。Desktop 已启动并观察到 784x588 窗口，但 Orca 无法附着 accessibility window 且截图不可用，因此 compact/wide、light/dark、所有迁移弹窗、实色 panel、暗色模式浅色 scrim、长本地化/CJK 文本、键盘提交和 Now Playing clearance 仍未进行运行时视觉验收，不作为通过项。
- [x] feat: Library Songs、专辑、艺人和 Search 支持页面级长按多选并按可见顺序加入播放列表；Settings 支持严格、有界、可移植的逻辑播放列表备份/恢复，唯一元数据匹配、陈旧预览拒绝、冲突命名与单事务恢复。iOS 短读越界检测缺陷已用 RED/GREEN 修复；controller review 要求的隐私证据已强化为 5 个导出条目各 16 个独特本地 canary（共 80 个），unchanged-production 集成 XML 为 1 test/0 failure/error/skip；精确 forced Task 1-8 JVM XML 为 272/0/0/0，Android-host adapter XML 为 10/0/0/0。Controller-visible independent re-review at `6af0ba85e629819937fcee60dad6d9909234ee45`: Spec Compliance PASS、Task Quality PASS，无 Critical/Important/Minor；OpenSpec 8.1-8.3、9.4 已完成，dedicated closure commit 后立即执行 final-head diff check 与 strict validation。Desktop/iOS 实际启动但 accessibility 无法附着且截图无法渲染，Android 无设备，因此长按/触摸、真实系统面板、compact/wide、light/dark、英文/中文/CJK、focus、bottom bar 与长预览视觉 QA 仍为未验证，不作为通过项；OpenSpec 9.1-9.3 保持开放，未归档。
- [x] feat: 播放列表详情默认保持普通播放行，长按进入全页编辑并以精确 occurrence ID 提供拖动、上下移动和移除；Shell Back 统一按弹窗、编辑、选择、Now Playing、直接路由返回的优先级处理。详情真实 LazyListState 接入共享 Bottom Bar 滚动/实测 clearance，HausDialog/HausLazyDialog 共享 theme-aware exterior。严格 OpenSpec、六类 focused JVM、完整 JVM/desktop/Android、Xcode 与 iOS simulator 自动验证通过，最终 Oracle APPROVE；compact/wide 像素外观、物理长按/拖动、Bottom Bar 动画体感、深浅色弹窗实机渲染、screen reader 与目标设备行为仍为未验证的人工后续证据。
- [x] 架构重构续篇：Library Back 现在由 `LibraryAppState` 以目的地实例身份、单一锁存 session 和 modal → edit → active-page selection → Now Playing → route 优先级统一编排；普通/system/predictive Back 共用协议，播放列表删除改为精确 displayed-destination invalidation（非 Back）。严格 OpenSpec、fresh focused Back/invalidation JVM（91 tests、0 failure/error/skip）、JVM/desktop/Android aggregate 与 iOS simulator 自动验证均已通过；早先 desktop `main.kt` 的 `App`/Koin DI 编译阻塞已由当前 aggregate 成功结果取代。设备/桌面实际 predictive gesture、Back 与删除视觉/交互仍需手动验证；未归档、未提交。
- [x] 架构重构前置：feature-first modularization 已在不改动源码的前提下完成历史证据对账。`architecture-refactor` 为 12/12；`package-organization` 的 0/5 checklist 明确保留为过时状态，已落地 package move 的 SHA 为 `f0310e5`、`06f8a16`、`adb1e3d`，不得重复移动其已落地路径。后续从模块治理基线开始。
- [x] 架构治理基线：Task 1.1 的 canonical skill、architecture document 与两份 ADR 已由 `55a4f72` 提交；经用户批准的 `spotlessApply` 格式化收尾由 `61ec5b7` 提交，独立 `spotlessCheck`、`detekt` 与 `:shared:jvmTest` 均通过。后续质量验证固定先运行 `spotlessApply`，再分别运行 `spotlessCheck` 与 `detekt`；无 waiver。
- [x] 架构治理基线后续：Task 1.1a 已为 root `AGENTS.md` 增加到 canonical architecture skill、document 和两份 ADR 的 link-only guidance；prerequisites、separate Spotless/Detekt、strict OpenSpec 与 diff 验证均通过。
- [x] 架构治理基线：Task 1.2 已添加 `build-logic.kmp.core`、`build-logic.kmp.feature.api` 与 `build-logic.kmp.feature.impl` 预编译 KMP conventions；TestKit 严格 RED/GREEN、full convention tests、Spotless、Detekt、strict OpenSpec 与 diff checks 均通过。OpenSpec 2.1 保持未完成，Task 1.3 architecture checker/allow-list 未开始。
- [x] 架构治理基线：Task 1.3 已添加 standalone root `architectureCheck`、code-owned allow-list 与覆盖依赖图/ownership/explicit API/KDoc/SQLDelight/iOS export 的 TestKit fixtures；focused/full tests、root task、Spotless、Detekt、strict OpenSpec 与 diff checks 均通过。OpenSpec 2.1/2.3 完成；2.4 root/CI quality entrypoint wiring 明确留待 Task 1.4。
- [x] 架构治理基线修复：保留 Task 1.3 support commit，并补齐 per-module ownership、sole shared SQLDelight owner、精确 ARCH diagnostic fixtures、token-aware Kotlin/KDoc/strict explicit API 与 TestKit configuration-cache coverage；Task 1.4 root/CI wiring 未开始，OpenSpec 2.4 保持未完成。
- [x] 架构治理 Task 1.3 policy amendment repair：按 `68154d7` 完成 package-preserving roots、actual-project-dependency declaration imports、真实 KMP/Compose resource/SQLDelight layout 与 typed explicit API；18-case TestKit RED/GREEN、full convention、architectureCheck cache reuse、Spotless、Detekt 和 strict OpenSpec 均通过。Task 1.4 root/CI wiring 仍未开始。
- [ ] 架构治理 Task 1.3 rejected：独立审查拒绝 `b2e2700` 的 architecture checker，记录六项 enforcement findings；不接受 architecture gate，OpenSpec 2.1/2.3 重新保持进行中，2.4 保持未完成，等待独立批准的 repair。
- [ ] 架构治理 Task 1.3 stable design：已记录 Gradle public-model + KSP 两层 checker 设计，替代 lexical/import-provider scanner；实现前仍需单独用户 review/approval，OpenSpec 2.1/2.3/2.4 保持未完成。
- [ ] 架构治理 Task 1.3 supported-API correction：architectureCheck 仅验证 public Gradle model，KSP 直接在 production compilation 报告 package/KDoc；修正 spec 仍待用户 review/approval，OpenSpec 2.1/2.3/2.4 保持未完成。
- [ ] 架构治理 Task 1.3 architecture-processor boundary：批准未来增加 root JVM `:architecture-processor` 作为 KSP artifact，不复用 convention JAR；仅 core/API production wiring，修正 written spec 仍待 review/approval，OpenSpec 2.1/2.3/2.4 保持未完成。
- [ ] 架构治理 Task 1.3 plan-scope correction：`dd772ed` 后已修正 plan 的 build-logic-only scope、inventory 与 staging 指令，使其仅允许已批准的 `:architecture-processor` boundary 及相关路径；Task 1.3 仍 rejected/in progress，仍须用户 review，OpenSpec 2.1/2.3/2.4 保持未完成。
- [ ] 架构治理 Task 1.3 RED-contract correction：rejected checker 保留为 baseline；先用 TestKit/KSP regressions 证明缺少 processor/core-API wiring，model RED 仅针对 stable public-model contract；仍待 user review，OpenSpec 2.1/2.3/2.4 保持未完成。
- [x] 架构治理 Task 1.3 stable checker：correction 后独立 Oracle final re-review PASS，OpenSpec 2.1/2.3 已完成。`ArchitectureCheckPluginFunctionalTest` XML 为 46 tests、0 skipped、0 failures、0 errors，包含 external canonical repository-built `:architecture-processor` JAR/SPI/KSP proof，以及 real Android-KMP host/device aggregate identity positive、authored-self、fail-closed cardinality cases；root `architectureCheck` 两次通过且 configuration cache reused，strict feature OpenSpec、`spotlessCheck`、`detekt` 与 `git diff --check` 均通过。`./init.sh` 按用户明确要求在超过 9000 秒后手动停止且未 rerun，因此完整 JVM/Android/desktop/iOS platform matrix 仍不确定且不声明通过；Task 1.4 root/CI quality wiring 为下一项且不在本次范围，feature-first modularization 整体未完成。
- [ ] 架构治理 Task 1.3 execution-plan synchronization：`4732db9` 的 stable-checker amendment 已同步到 executable plan、Superpowers feature spec 与 OpenSpec references；Task 1.3/OpenSpec 2.1/2.3/2.4 均保持未完成，implementation 仍须 explicit user review gate。
- [ ] 架构治理 Task 1.3 SQLDelight convention boundary：选择由 dedicated `build-logic.sqldelight` convention 负责应用/配置 SQLDelight；其 build-logic classpath 使用 `implementation("app.cash.sqldelight:gradle-plugin:2.3.2")`，`:shared` 不再独立应用插件，root checker 可 type-safe 检查 public SQLDelight model。仅改变 Gradle implementation ownership，保留单一 physical database、既有 configuration/schema/migrations/package/name/platform drivers；需要 former-classloading RED、physical-ownership fixture 和 real-root architectureCheck GREEN。无 reflection/internals/classloader probing/build-script parsing；Task 1.4 wiring 与 automatic product-module migration 均不在范围内。
- [ ] 架构治理 Task 1.3 normalized registry boundary：shared build-logic owns immutable normalized `ArchitectureModelRegistry`; Android/Compose/SQLDelight/KMP conventions own public plugin API/classpath access and publish records, root checker consumes records plus existing inputs only. Android Components callbacks 仅提供 main production static roots；module namespace 来自 concrete public Android DSL `CommonExtension.namespace`（`ApplicationExtension` 或 `LibraryExtension`），不来自 AGP 9.3 `Variant`，且禁止 casts/reflection/task-artifact internals/per-variant namespace claim，排除 test/androidTest/test-fixture。Compose 记录标准/custom roots 与 convention-declared configured namespace；blank declaration 作为 invalid registry fact 保留以产生 deterministic `ARCH-RESOURCE` diagnostics，且不传给 Compose Resources；不声称 effective namespace 或 independent introspection。KSP 仅 real registered JVM/Android/native consumer 是 tooling（generic `ksp` 仅 single-platform JVM/Android），processor 为 compilation-local、initial-input-only、sorted/deduplicated relative paths，不做 whole-project aggregation。RED/GREEN 覆盖 variants/resources/spoofed KSP/generated-test-local/declaration forms/exact diagnostics/cache reuse；SQLDelight physical ownership、no reflection/internals/buildscript parsing/KSP output consumption 和 Task 1.4 exclusion 不变。
- [ ] 架构治理 Task 1.3 rejected contract amendment：SQLDelight artifact collections 作为 root task inputs 并在 execution 派生 current physical artifacts，driver 仅 documented production direct dependency；Compose 只通过 public `customDirectory` 配置/发布 declared roots，不声称 effective-root observation；独立 Android-KMP convention 以 public namespace/main-variant model 发布 enabled main roots。KSP 使用 initial `getNewFiles()`、same-round diagnostics 和 relative path/source position/qualified-name identity；self edge 同时保留 `ARCH-EDGE`/one-node `ARCH-CYCLE`。fixture/evidence 必含 former classloader RED/GREEN、cache-invalidated artifacts、production driver、Compose/Android-KMP、real KMP JVM SPI、Android/native registration only、pre-existing generated source 与 KDoc overload/path。Task 1.4 wiring 不变，Task 1.3 继续 rejected/in progress。
- [ ] 架构治理 Task 1.3 AGP synthetic-self-edge correction：public AGP 9.3.1 evidence confirms every controlled `ApplicationVariant` test-component compile/runtime configuration has `isCanBeDeclared == true`; the prior non-declarable premise is removed because declarable status provides no dependency provenance. Android applications use public `ApplicationVariant` test `Component.compileConfiguration`/`runtimeConfiguration`; Android-KMP libraries use public `KotlinMultiplatformAndroidLibraryTarget.compilations` plus public `KotlinMultiplatformAndroidHostTestCompilation`/`KotlinMultiplatformAndroidDeviceTestCompilation` `compileDependencyConfigurationName`/`runtimeDependencyConfigurationName`. After normalization, root suppresses a direct self record only for an exact published identity with exactly one distinguishable direct self record in that configuration, otherwise none. Authored supported declarable-bucket dependencies remain checked and explicit self edges retain exact `ARCH-EDGE`/one-node `ARCH-CYCLE`; equal authored mutation on the exact AGP-owned configuration remains unsupported without public provenance. Prohibitions remain name inference, blanket test filtering, attribute guessing, reflection/AGP internals, and task/artifact/output/resolved-classpath inspection. Real Android RED captures exact three synthetic self edges/cycle; GREEN removes only those while retaining canonical main resources, authored negative control, and cardinality fail-closed control. Task 1.3/OpenSpec 2.1/2.3 remain unchecked; final acceptance is blocked on the real Android-KMP aggregate identity fixture and docs/evidence reconciliation.
- [x] 架构治理 Task 1.4 accepted：root `check`/`qualityCheck` now enforce `architectureCheck`, and provider-backed `qualityCheck` aggregates Detekt/Spotless checks across all projects. Oracle review PASSed with no Critical/Important/Minor findings; exact-diagnostic TestKit entrypoints, serializable child `Copy`-sentinel cache-reuse proof, serial processor JAR/full convention evidence (functional XML 50/0/0/0), 85-task production quality, standalone Spotless/Detekt/Spotless apply, strict OpenSpec validation, and diff hygiene passed. Unfiltered `quality.yml` runs only canonical qualityCheck for PRs/main. Root quality cache reuse and the full platform matrix are not claimed: unchanged Spotless invalidation remains nonblocking, and `./init.sh` was not rerun after its user-authorized >9000-second stop. Task 1.5+ and OpenSpec 8.4 remain pending.
- [x] 架构重构 Slice 2 Task 2.1 accepted：仅将 package-stable immutable `Track`、`TrackAccent`、`LibrarySnapshot`、`PlayableTrack` 与 `AudioSource` 提取至 `:core:model`；`Track`/`PlayableTrack` 保留 nullable artwork `ByteArray` 的内容 equality/hashCode，`AudioSource.stableKey` 保留且未添加 `PlayableTrack.stableKey`。独立 Oracle final re-review PASS，无 Critical/Important/Minor；`:core:model` allTests 两次通过（JVM/Android host/iOS simulator arm64 XML 均 6/0/0/0）及 architecture/shared JVM、quality、strict OpenSpec、diff checks 均通过。Task 2.2+ 与 OpenSpec 3.1/3.2/3.3 仍 pending；完整平台矩阵不声明通过。
- [x] 架构重构 Slice 2 Task 2.2 accepted：`:core:ui` 现拥有 generic `ArtworkImage` role/cache-key rendering、完整 `ArtworkDecoder` expect/actual/cache、Back/top-app-bar/clickable helpers、theme mode/palette policy、HausDialog family、英中文 Back resources 和 public generated `Res`；private `RhythHausTheme` composition、TrackArtwork loading、ThemePreferenceStore 及 feature UI/state/routes/gestures/scrubber/glass chrome 保留在 `:shared`，后者以 `api(projects.core.ui)` 依赖，iOS framework 不 export core UI。specification re-review 和 initial final quality review 无 Critical/Important source findings；cleanup verification 接受两项 source fix 并要求修正上述 theme ownership ledger；final cleanup-ledger re-review PASS 且无 findings。早先严格 allTests/shared JVM/architecture、desktop、Android packaged resources、iOS simulator compile、Xcode、Spotless/Detekt/OpenSpec/diff gates 均通过（XML: core UI 51、shared JVM 562，零 failure/error/skip）；cleanup 后 focused JVM/architecture 31-task gate 与 all-target core UI/shared JVM/architecture 93-task gate 均通过并 stored configuration cache。OpenSpec 3.1/3.2/3.3 complete；`docs/architecture.md`/ADR 0001 已足够，不作 speculative churn。下一安全动作：Task 3.1 core database inventory。Chinese/Android/iOS runtime、platform decoder correctness 和 linked iOS runtime resource lookup 未验证；`./init.sh` 未在先前用户停止超过 9000 秒后重跑。
- [x] 架构重构 Slice 3 Task 3.1 accepted：`:core:database` 原子拥有六个 `.sq`、`1.sqm`、v1 `1.db`、generated/runtime SQLDelight surface、expect/actual drivers 与数据库测试；SQL/FK/history/name/byte compatibility 保持不变。`:shared` 保留 repositories、DI、public `LibraryDatabaseContext` facade 与 `-lsqlite3`，通过 `api(projects.core.database)` 暴露，无 app direct core edge、iOS framework export 或 core 对 shared/model 依赖。最终 architecture reviewer PASS 无 Critical/Important/Minor；architecture XML 52/0/0/1 expected skip、convention XML 102/0/0/1 expected skip，database JVM/Android host/iOS 3/1/1 与 shared JVM 559 均零 failure/error/skip；quality/OpenSpec/diff/consuming compilation 均通过。Task 3.2 的 conditional `:core:platform` inventory 与完整 runtime/init matrix defer；`./init.sh` 未在用户此前 >9000s 停止后重跑，Android/iOS UI runtime、linked iOS app/resource 与 desktop runtime 未执行。
- [x] 架构重构 Slice 3 Task 3.2 accepted：`07da78e`（base `ba500c3`）仅将 package-stable `currentTimeMillis()` / `uuid4()` expect/actual family 提取至 `:core:platform`；time 服务 Library scanning + Playlist backup，UUID 服务 Library scanning + Playback。`:shared` 以 `api(projects.core.platform)` 暴露，iOS framework 不 export core platform，core platform 无 production dependencies，source/metadata/playback engine/dispatch/persistence/theme/backup-document/application-context 等 feature-specific platform seams 保持 feature/shared-owned。Independent reviewer PASS/APPROVED，无 Critical/Important/Minor。Controller integration first run 226 tasks/47s cache stored、identical repeat 1s cache reused；core platform JVM/Android host/iOS XML 2/2/2 与 shared JVM 559 均零 failure/error/skip，Spotless/Detekt 通过。Final staged strict OpenSpec validation 与 diff checks 在 closure 后通过，包含 force-added Task 3.2 evidence files。`./init.sh`、UI/runtime launch、linked iOS app/resource lookup 与 desktop runtime 未运行或声明；下一安全动作：Task 4.1 API publication。


## Task 6.1 — Library feature extraction to `:feature:library:impl`
Completed `741f5eb`. `:shared` retains composition/routing/playback/Koin/iOS facade.

- [x] 架构重构 Slice 7 Task 7.1-7.3 accepted：`:shared` 清理至 thin facade 角色（`App()`、root shell、route/Back、lifecycle、Koin、`MainViewController` + retained session/theme/playback-factory/backup-ABI/selection/Now-Playing/formatting helpers）。删除 dead `Logger`/`Platform`/`getPlatform`（零 call site）；新增 `ThinSharedInventoryTest` 断言 31 文件 facade 集（临时 dead file 证明 RED）；新增 `FeatureScaffoldPlugin`（`build-logic.feature-scaffold`）仅生成真实请求模块结构 + 3-test functional 覆盖 + `skills/kmp-architecture/SKILL.md` 文档。shared JVM/Android/iOS 编译、`:build-logic:convention:test`、`architectureCheck`/`qualityCheck`、`./init.sh`、strict OpenSpec、diff checks 均通过。Deferred：package renames、Dependency Analysis Gradle Plugin、runtime/device/visual。
- [x] Playback state synchronization: iOS observes AVAudioSession interruption and output-route events with main-thread-confined, generation-guarded recovery and lifecycle cleanup; macOS observes Core Audio device snapshots, distinguishes device removal from benign output switches, and safely drains route listeners before native teardown. Implementation commits: `3593130`, `d36cafc`, `c850abf`, `30c9817`. Focused core playback JVM/iOS tests, native helper rebuilds, Xcode consumer build, Spotless, and Detekt passed. Shared iOS remains blocked by the unrelated `LibraryPlaybackSelectionTest.savedPlaylistSelectionStartsTheSelectedDuplicateOccurrence` timeout; `architectureCheck` remains blocked by existing `dependencyUpdatesAggregation` cycle diagnostics. Physical AirPods takeover, calls, Siri, and real route removal remain unverified.
