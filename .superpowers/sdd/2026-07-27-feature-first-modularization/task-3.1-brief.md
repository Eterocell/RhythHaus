## Task 3.1: Move The Sole SQLDelight Owner Atomically

**Scope:** Slice 3 database. No schema/name/history/FK changes.

**Existing files:** `shared/build.gradle.kts:L142-L152,L195-L207,L246-L248`; `shared/src/commonMain/kotlin/com/eterocell/rhythhaus/library/LibraryDatabase.kt:L5-L10`; `shared/src/commonMain/sqldelight/com/eterocell/rhythhaus/library/{RhythHausDatabase,LibrarySource,LibraryTrack,Playlist,ScanSession,ScanError}.sq`; `shared/src/commonMain/sqldelight/migrations/1.sqm`; `shared/src/commonMain/sqldelight/databases/1.db`; platform `LibraryDatabase.{android,jvm,ios}.kt`; database tests `LibraryDatabaseAndroidHostTest.kt` and `LibraryDatabaseIosTest.kt`; and shared repository behavior tests `SqlDelightLibraryRepositoryJvmTest.kt` and `PlaylistSqlDelightRepositoryJvmTest.kt`.

**Target files:** `core/database/build.gradle.kts`; matching common/platform database sources and database-owned tests; moved SQLDelight inputs under `core/database/src/commonMain/sqldelight/`; `core/database/src/jvmTest/kotlin/com/eterocell/rhythhaus/library/ExistingDatabaseMigrationTest.kt`; updated settings/shared build files and allow-list. Repository mapping, mutation, and persistence behavior tests remain in `:shared`.

- [ ] Make RED executable in two stages. First run `./gradlew :core:database:jvmTest --tests '*ExistingDatabaseMigrationTest' --configuration-cache`; expected RED: module/task absent. Then add only module/test scaffolding and database-owned tests while production implementation is absent; expected RED: compilation/test failure on the missing database seam/generated API. Separately invert the baseline TestKit SQLDelight owner fixtures and prove they fail while `:shared` remains owner. Do not use `:shared` repositories in core tests.
- [ ] The core JVM migration test uses the checked-in v1 fixture, raw JDBC/SQL only for initial inspection/seeding, moved production JVM `LibraryDatabase` to migrate, and generated `RhythHausDatabase` queries/driver to prove preserved source/track rows, `Schema.version` and current `PRAGMA user_version`, a valid playlist entry, invalid missing-track FK rejection, and `ON DELETE CASCADE`. Include compile-time generated package/name coverage and constant/database filename assertions. The core test must not depend on `:shared` or `:core:model`. Move/rewire database-owned v1 migration and legacy-v0 bootstrap tests, Android callback testing, and iOS FK testing with their actuals; repository behavior tests stay in `:shared`.
- [ ] Move `.sq`, `1.sqm`, `1.db`, SQLDelight package config, `LibraryDatabase` expect/actuals, and Android/JVM/iOS drivers atomically. `:core:database` uses `api(libs.sqldelight.runtime)`, platform-driver `implementation` dependencies, and a test-scoped SQLite driver, with no Compose/resources. Preserve package `com.eterocell.rhythhaus.library`, generated `RhythHausDatabase` name, SQLite dialect/schema output, and `rhythhaus.db`; remove the unused SQLDelight coroutine extension from `:shared` and preserve the shared framework `-lsqlite3` linkage. Keep `SqlDelightLibraryRepository.kt` and `SqlDelightPlaylistRepository.kt` outside core database.
- [ ] Split Android context ownership: public `LibraryDatabaseContext` remains in `:shared` because unrelated platform services consume it. Its existing `applicationContext` setter forwards to a documented public Android-only `:core:database` initializer whose backing holder is private and stores the application context. This is the only transitional `:shared -> :core:database` coupling and does not implement Task 3.2.
- [ ] `:shared` uses `api(projects.core.database)` because public repository constructors expose `LibraryDatabase`; no direct app-to-core edge is added and the iOS framework does not use `export(projects.core.database)`.
- [ ] Move the architecture-checker baseline TestKit owner to `:core:database`, update allow-list ownership, and make `ArchitectureCheckTask` derive the expected owner from policy rather than a duplicate hard-coded string. Retain one/missing/two/arbitrary/spoofed owner coverage and configuration-cache reuse. Turn both the database and owner fixtures GREEN only after the atomic production move.
- [ ] The database seam remains explicit and package-stable:
  ```kotlin
  public expect class LibraryDatabase { public val driver: SqlDriver; public val database: RhythHausDatabase }
  ```
- [ ] Run `./gradlew :core:database:jvmTest :core:database:generateCommonMainRhythHausDatabaseInterface --configuration-cache`; expected GREEN. Then run the database-owned Android/iOS tests, architectureCheck, and the approved broader verification. Task 3.2 and later feature modules are out of scope.
- [ ] Acceptance inventory: the six `.sq` files, migration, schema DB, package config, expect/actual drivers, public SQLDelight runtime API, and platform driver implementations move together; SQL statements, database name, generated package, SQLite dialect/schema output, FKs, and migration history are unchanged.
- [ ] Commit with `git add core/database shared settings.gradle.kts build-logic && git commit -m "refactor: extract core database"`.
