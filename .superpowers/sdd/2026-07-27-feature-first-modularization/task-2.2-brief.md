## Task 2.2: Extract Core UI

**Scope:** Slice 2. Reusable visual primitives/theme/generic-artwork abstractions only; shared retains library artwork loading.

**Existing files:** `shared/src/commonMain/kotlin/com/eterocell/rhythhaus/ui/ArtworkImage.kt`; `ui/ArtworkDecoder.kt`; `ui/BackChip.kt`; `ui/HausClickable.kt`; `ui/HausDialog.kt`; `ui/RhythHausTopAppBar.kt`; `theme/Theme.kt`; `theme/HausColors.kt`; English and Chinese `back`/`back_button` Compose resources; `shared/src/commonMain/kotlin/com/eterocell/rhythhaus/library/ui/LibraryAppState.kt:L16-L336`; `shared/src/commonMain/kotlin/com/eterocell/rhythhaus/library/ui/LibraryNavigation.kt:L185-L330`; `shared/src/androidMain/kotlin/com/eterocell/rhythhaus/ui/ArtworkDecoder.android.kt`; `shared/src/jvmMain/kotlin/com/eterocell/rhythhaus/ui/ArtworkDecoder.jvm.kt`; `shared/src/iosMain/kotlin/com/eterocell/rhythhaus/ui/ArtworkDecoder.ios.kt`.

**Target files:** `core/ui/build.gradle.kts`; matching `core/ui/src/{commonMain,androidMain,jvmMain,iosMain}/kotlin/...`; `core/ui/src/commonMain/composeResources/` for its controlled `back`/`back_button` namespace; `core/ui/src/{commonTest,jvmTest}/kotlin/...` artwork/dialog contracts; package-stable shared-side artwork-loader source/tests; updated consumers/allow-list.

- [ ] Create failing-first contracts from existing `shared/src/commonTest/kotlin/com/eterocell/rhythhaus/ui/ArtworkImageTest.kt`, `ArtworkCacheTest.kt`, and `HausDialogTest.kt`, plus `shared/src/jvmTest/kotlin/com/eterocell/rhythhaus/ui/HausDialogSemanticsJvmTest.kt`: `:core:ui` receives generic artwork cache/key and theme contracts plus JVM `BackChip`/top-app-bar and dialog semantics; `:shared` retains and runs library-loader contracts. Run `./gradlew :core:ui:allTests --configuration-cache`; expected RED: absent module/task.
- [ ] Run bounded inventory discovery before moves: `rg --files shared/src/commonMain/kotlin/com/eterocell/rhythhaus/{ui,theme} shared/src/{androidMain,jvmMain,iosMain}/kotlin/com/eterocell/rhythhaus/ui`. Acceptance inventory is reusable primitive/theme/generic-artwork files, `ui/HausClickable.kt`, `ui/HausDialog.kt`, paired `ArtworkDecoder` actuals, matching dialog tests, and only English/Chinese `back`/`back_button` resources; exclude all `library/ui/**`.
- [ ] Split `ArtworkImage.kt` by symbol ownership without package rename: move `ArtworkImageRole`, `artworkMemoryCacheKey`, and generic `ArtworkImage` to `:core:ui`; keep `LocalTrackArtworkLoader`, `TrackArtworkLoadState`, `initialTrackArtworkLoadState`, `loadTrackArtworkState`, `rememberLazyTrackArtworkState`, `LazyTrackArtworkImage`, their `TrackArtwork` dependency, and loader tests in `:shared`, using a package-stable shared-side file if needed.
- [ ] Add module/build dependencies and move exactly the accepted core inventory. Move `ui/HausDialog.kt`, `HausDialogTest.kt`, and `HausDialogSemanticsJvmTest.kt` to matching `:core:ui` source sets; make only `HausDialog` and `HausLazyDialog` public with explicit `Unit` return types and public KDoc, while keeping their presentation policy internal. Move the existing English and Chinese `back`/`back_button` resources only to `:core:ui`'s controlled Compose resource namespace, remove shared duplicates, and preserve their exact values, accessibility, and click behavior. `:shared` uses `api(projects.core.ui)` to preserve the Kotlin facade; the iOS framework must not export `:core:ui`. Never move `library/ui/**`, `ThemePreferenceStore` or its actuals, feature state/routes/navigation, `MusicProgressScrubber`, `LiquidGlassChrome`, or unlisted feature UI.
- [ ] Run `./gradlew :core:ui:allTests :core:ui:compileKotlinJvm --configuration-cache`; expected GREEN. Run Android/desktop/iOS compilation, architectureCheck, Detekt, and Spotless.
- [ ] Commit with `git add core/ui shared settings.gradle.kts build-logic && git commit -m "refactor: extract core UI primitives"`.
